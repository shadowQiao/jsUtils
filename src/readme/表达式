表达式：
    是由运算元和运算符(可选)构成，并产生运算结果的语法结构。
单值表达式：不使用运算符
    基本表达式(es5)：
        1.this、null、arguments等关键字
        2.变量
        3.字面量(数字、布尔值、字符串、正则)
        4.分组表达式
    复杂表达式：
        1.对象初始化，数组初始化
        [expr1, expr2, expr3]
        {
            expr1: expr2,
            expr3: expr4
        }
        {
            [expr1]: expr2,
            [expr3]: expr4
        }
        2.函数定义
        3.属性访问
        4.函数调用
        5.对象创建
        new expr1()
复合表达式：运算符将单值表达式合在一起


